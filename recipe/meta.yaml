{% set name = "pyportfolioopt" %}
{% set version = "1.5.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyportfolioopt-{{ version }}.tar.gz
  sha256: 6e52ff150a6ec401c0b329bd344b3e2eacb419c927500279eb11307467f00513

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - poetry
    - python >=3.6,<4.0
  run:
    - cvxpy >=1.1.10,<2.0.0
    - numpy >=1.12,<2.0
    - pandas >=0.19
    - python >=3.6,<4.0
    - scipy >=1.3,<2.0
    - matplotlib-base >=3.2.0,<4.0
    - cvxpy >=1.1.10,<2.0
    - cvxopt >=1.2,<2.0
    - scikit-learn >=0.24.1,<1.0

test:
  imports:
    - pypfopt
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/robertmartin8/PyPortfolioOpt
  summary: Financial portfolio optimization in python
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - atrawog
